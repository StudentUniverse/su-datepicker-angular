describe('su.datepicker.directives.suMonthDirective', function(){
    beforeEach(module('su.datepicker.directives.suMonthDirective'));

    // must use inject to run config/run of module
    it('should be able to load', inject(function(){
        expect(true).toBe(true);
    }));

});
